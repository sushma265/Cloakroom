# Free token from https://huggingface.co/settings/tokens
# Needs the "Make calls to Inference Providers" permission enabled.
HF_TOKEN=

# Optional overrides — only needed if a default model/Space becomes
# unavailable and you want to swap it without touching code.
# HF_TEXT_MODEL=openai/gpt-oss-120b
# HF_TRYON_SPACE=yisol/IDM-VTON
